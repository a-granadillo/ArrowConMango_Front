<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows cleartext (HTTP) traffic only to known local development hosts,
  since ApiConfig.baseUrl defaults to http://10.0.2.2:3000 (Android emulator
  loopback to the host machine). Since Android 9 (API 28), cleartext traffic
  is blocked by default; a blanket usesCleartextTraffic="true" would allow it
  app-wide, including in release builds pointed at a real production host.

  When running against a LAN IP (physical device, using the dart define flag
  for API_BASE_URL), add that IP below or override this file per environment.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
